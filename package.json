{
  "name": "government-frontend-survey",
  "version": "1.0.0",
  "description": "Sharing the results of the Government Frontend Survey",
  "main": "results.js",
  "scripts": {
    "build": "node generate-comparisons.js && node generate-results.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "MIT",
  "dependencies": {
    "csvjson": "^5.1.0",
    "github-slugger": "^1.2.1",
    "outdent": "^0.7.0"
  }
}
